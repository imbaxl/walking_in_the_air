---
tags: 数学分析
---


| 排序   | 内容          |
| ---- | ----------- |
| 定义1  | 第二类曲线积分     |
| 命题2  | 方向性         |
| 命题3  | 线性性         |
| 命题4  | 路径可加性       |
| 定理5* | 第二类曲线积分计算公式 |
| 例题6  |             |
| 定义7  | 双侧曲面        |
| 定义8  | 第二类曲面积分     |
| 命题9  | 方向性         |
| 命题10 | 线性性         |
| 命题11 | 曲面可加性       |
| 例题12 |             |


**** 


# 1. 曲线积分（做功大小）

较早也有做过笔记：[[第二类曲线积分(旧)]]

>[!dfn] 定义1（第二类曲线积分）
>设 $L$ 为一条定向的光滑曲线，起点为 $A$，终点为 $B$，在 $L$ 上每一点取单位切向量 $\boldsymbol{\tau}= (\cos \alpha, \cos \beta, \cos \gamma)$，使它与 $L$ 的定向相一致。设
>$$
>\boldsymbol{f}(x,y,z) = P(x,y,z)\boldsymbol{i} + Q(x,y,z)\boldsymbol{j} + R(x,y,z)\boldsymbol{k}
>$$
>是 $\boldsymbol{f}$ 定义在 $L$ 上的向量值函数，则称
>$$
>\int_L f \cdot  \boldsymbol{\tau} \mathrm{d}s = \int_L \left[ P(x,y,z)\cos \alpha + Q(x,y,z)\cos \beta + R(x,y,z)\cos \gamma \right] \mathrm{d}s
>$$
>为 $\boldsymbol{f}$ 在 $L$ 上的**第二类曲线积分**（如果右面的第一类曲线积分存在的话）

^930526


>[!prp] 命题2（方向性） 
>设向量值函数 $\boldsymbol{f}$ 在定向的分段光滑曲线 $L$ 上的第二类曲线积分存在。记 $-L$ 是定向曲线 $L$ 的反向曲线，则 $\boldsymbol{f}$ 在 $-L$ 上的第二类曲线积分也存在，且成立
>$$
>\int_{L} \boldsymbol{f} \cdot  \boldsymbol{\tau}\ \mathrm{d}s = - \int_{-L} \boldsymbol{f} \cdot  \boldsymbol{\tau} \ \mathrm{d}s.
>$$
注意这个等式两边的 $\boldsymbol{\tau}$ 是方向相反的.

^3cd689



>[!prp] 命题3（线性性）
>设两个向量值函数 $\boldsymbol{f}, \boldsymbol{g}$ 在定向的分段光滑曲线 $L$ 上的第二类曲线积分存在，则对于任何常数 $\alpha, \beta$，$\alpha \boldsymbol{f} + \beta \boldsymbol{g}$ 在 $L$ 上的第二类曲线积分也存在，且成立
>$$
>\int_L (\alpha \boldsymbol{f} + \beta \boldsymbol{g}) \cdot \boldsymbol{\tau} \ \mathrm{d}s = \alpha \int_L \boldsymbol{f} \cdot \boldsymbol{\tau} \ \mathrm{d}s + \beta \int_L \boldsymbol{g} \cdot \boldsymbol{\tau}\ \mathrm{d}s.
>$$





>[!prp] 命题4（路径可加性）
>设定向分段光滑曲线 $L$ 分成了两段 $L_1$ 和 $L_2$，它们与 $L$ 的取向相同（这时记为 $L = L_1 + L_2$），如果向量值函数 $\boldsymbol{f}$ 在 $L$ 上的第二类曲线积分存在，则它在 $L_1$ 和 $L_2$ 上的第二类曲线积分也存在。反之，如果 $\boldsymbol{f}$ 在 $L_1$ 和 $L_2$ 上的第二类曲线积分存在，则它在 $L$ 上的第二类曲线积分也存在，且成立
>$$
>\int_L \boldsymbol{f}\cdot \boldsymbol{\tau} \ \mathrm{d}s = \int_{L_1} \boldsymbol{f} \cdot  \boldsymbol{\tau} \ \mathrm{d}s + \int_{L_2} \boldsymbol{f} \cdot \boldsymbol{\tau}\ \mathrm{d}s.
>$$


第二类曲线积分计算方法

考虑定义 [[第一类线面积分#^d10cc8|第一类曲线积分]] ，再对比 [[#^930526|第二类曲线积分]] 的定义，弧长微元都是一样的，不同的地方在于第二类曲线积分的函数是**向量值函数**（矢量函数），需要在每个任取的点 $(\xi_{i},\eta_{i},\zeta_{i})$ 上作用在这个点的切向量上。所以第二类曲线积分主要解决函数值的计算上。

>[!thm] 定理5*（第二类曲线积分计算公式）
$\displaystyle\int_{L} P(x,y,z)\,\ \mathrm{d}x + Q(x,y,z)\,\ \mathrm{d}y + R(x,y,z)\,\ \mathrm{d}z$
$=\displaystyle \int_{L} \left[ P(x,y,z) \cos\alpha + Q(x,y,z) \cos\beta + R(x,y,z) \cos\gamma \right] \ \mathrm{d}s$$= \displaystyle\int_{a}^{b} \left[ P(x(t),y(t),z(t))x'(t) + Q(x(t),y(t),z(t))y'(t) + R(x(t),y(t),z(t))z'(t) \right] \ \mathrm{d}t.$

- **弧长微元转参数微元**：其实对于我自己而言，衡量对第二类曲线积分的理解，着重就是这三个公式的互换过程，也就是有三个角度看待这个积分。比如第一个等号 $(\cos\alpha,\cos\beta,\cos\gamma)$ 也是一个连续向量值函数，这是从 Riemann 和求极限中转变的，并且它等于 $\dfrac{(x'(t),y'(t),z'(t))}{\sqrt{x'(t)^2+y'(t)^2+z'(t)^2}}$，它可以用 $\ \mathrm{d}s$ 约掉分母，即 
$$
\begin{align}& (\cos\alpha,\cos\beta,\cos\gamma)\ \mathrm{d}s\\ =&\dfrac{(x'(t),y'(t),z'(t))}{\sqrt{x'(t)^2+y'(t)^2+z'(t)^2}} \sqrt{x'(t)^2+y'(t)^2+z'(t)^2}\ \mathrm{d}t \\ =&(x'(t)\ \mathrm{d}t,y'(t)\ \mathrm{d}t,z'(t)\ \mathrm{d}t) \end{align}
$$ ^8467b1



>[!prp] 例题6.1
>计算 $\displaystyle\int_{L} y^{2}\,\ \mathrm{d}x + x^{2}\,\ \mathrm{d}y$，其中 $L$ 为：(1) 圆周 $x^{2}+y^{2}=R^{2}$ 的上半部分，方向为逆时针方向；(2) 从点 $M(R,0)$ 到点 $N(-R,0)$ 的直线段.




>[!prp] 例题6.2
>求空间中一质量为 $m$ 的物体沿某一光滑曲线 $L$ 从 $A$ 点移动到 $B$ 点时，重力所做的功.



>[!prp] 例题6.3
>计算 $\displaystyle\int_{L} (y^{2}-z^{2})\,\ \mathrm{d}x + (z^{2}-x^{2})\,\ \mathrm{d}y + (x^{2}-y^{2})\,\ \mathrm{d}z$，其中 $L$ 为球面 $x^{2}+y^{2}+z^{2}=1$ 在第一卦限部分的边界，当从球面外面看时为顺时针方向.




# 2. 曲面积分



>[!dfn] 定义7（双侧曲面）
>设 $\Sigma$ 是一张光滑曲面，$P$ 为 $\Sigma$ 上任一点，$\Gamma_{P}$ 是过 $P$ 点且不超过曲面边界的任意一条闭曲线。取定 $\Sigma$ 在 $P$ 点的一个单位法向量，让它沿 $\Gamma_{P}$ 连续移动，使它与所过之点处的一个单位法向量连续地相合。如果当它再回到 $P$ 点时，法向量的指向仍与原选的方向相同，则称 $\Sigma$ 为**双侧曲面**.


## 2.1. 准备
设双侧曲面 $\Sigma$ 的方程为
$$
x = x(u,v),\,y = y(u,v),\, z = z(u,v), \quad(u,v) \in D.
$$

这里 $D$ 为 $uv$ 平面上具有分段光滑边界的区域。进一步假设 $x,y,z$ 对 $u$ 和 $v$ 有连续偏导数，且相应的 Jacobi 矩阵

$$
J = \begin{pmatrix} \dfrac{\partial x}{\partial u} & \dfrac{\partial x}{\partial v} \\ \dfrac{\partial y}{\partial u} & \dfrac{\partial y}{\partial v} \\ \dfrac{\partial z}{\partial u} & \dfrac{\partial z}{\partial v} \end{pmatrix}
$$

总是满秩的。这时曲面 $\Sigma$ 是光滑的。

## 2.2. 将$\ \mathrm{d}S$ 转成$\ \mathrm{d}u\ \mathrm{d}v$
参考 [[#^8467b1|弧长微元转参数微元]]，此处也对应制作一份 **面积微元转参数微元**。从第一类曲面积分中得到 $\ \mathrm{d}S=\sqrt{EG-F^2}\ \mathrm{d}u\ \mathrm{d}v$，

定义一个第二类曲面积分：$\displaystyle\int_{\Sigma} \boldsymbol{f} \cdot \boldsymbol{n} \ \mathrm{d}S$，$\boldsymbol{n}$ 是曲面 $\Sigma$ 在某点的单位法向量，而法向量则是通过 $\boldsymbol{r}_{u},\boldsymbol{r}_{v}$ 的叉积（叉积的几何意义）得到，单位化
$$
\begin{align} \boldsymbol{n}=(\cos\alpha,\cos\beta,\cos\gamma)&=\dfrac{\boldsymbol{r_{u}\times \boldsymbol{r}_{v}}}{\|  \boldsymbol{r_{u}\times \boldsymbol{r_{v}}}\|} \\ &=\dfrac{(\dfrac{\partial{(y,z)}}{\partial{(u,v)}},\dfrac{\partial{(z,x)}}{\partial{(u,v)}},\dfrac{\partial{(x,y)}}{\partial{(u,v)}})}{\pm \sqrt{(\boldsymbol{r}_{u}\cdot \boldsymbol{r}_{u})(\boldsymbol{r}_{v}\cdot \boldsymbol{r}_{v})-(\boldsymbol{r}_{u}\cdot \boldsymbol{r}_{v})^2}} \\&=\dfrac{(\dfrac{\partial{(y,z)}}{\partial{(u,v)}},\dfrac{\partial{(z,x)}}{\partial{(u,v)}},\dfrac{\partial{(x,y)}}{\partial{(u,v)}})}{\pm \sqrt{EG-F^2}} \end{align}
$$
从而有
$$
\begin{align} \displaystyle\int_{\Sigma} \boldsymbol{f} \cdot \boldsymbol{n} \ \mathrm{d}S &=\displaystyle\int_{\Sigma }\boldsymbol{f}  (\cos\alpha,\cos\beta,\cos\gamma) \ \mathrm{d}S\\&=\displaystyle\int_{\Sigma }\boldsymbol{f} \cdot(\boldsymbol{r_{u}\times \boldsymbol{r}_{v}}) \ \mathrm{d}u\ \mathrm{d}v  \\ \end{align}
$$

# 3. 将 $\boldsymbol{f}$ 分成 $P,Q,R$

向量值函数 $\boldsymbol{f}$ 可以分为 $P(x,y,z),\,Q(x,y,z),\,R(x,y,z)$




>[!dfn] 定义8（第二类曲面积分）
>设 $\Sigma$ 为定向的光滑曲面，曲面上的每一点指定了单位向量场 $\boldsymbol{n} = (\cos \alpha, \cos \beta, \cos \gamma)$。如果 $f(x,y,z) = P(x,y,z)i + Q(x,y,z)j + R(x,y,z)k$ 是定义在 $\Sigma$ 上的向量值函数，称
>$$
>\iint_{\Sigma} \boldsymbol{f} \cdot \boldsymbol{n} \ \mathrm{d}S = \iint_{\Sigma} \left[ P(x,y,z)\cos \alpha + Q(x,y,z)\cos \beta + R(x,y,z)\cos \gamma \right] \ \mathrm{d}S
>$$
为 $\boldsymbol{f}$ 在 $\Sigma$ 上的第二类曲面积分（如果右面的第一类曲面积分存在）。




>[!prp] 命题9（方向性）
>设向量值函数 $\boldsymbol{f}$ 在定向的光滑曲面 $\Sigma$ 上的第二类曲面积分存在，记 $-\Sigma$ 为与 $\Sigma$ 取相反侧的曲面，则 $f$ 在 $-\Sigma$ 上的第二类曲面积分也存在，且成立
>$$
>\iint_{-\Sigma} \boldsymbol{f} \cdot \boldsymbol{n} dS = - \iint_{\Sigma} \boldsymbol{f} \cdot \boldsymbol{n} dS.
>$$
>注意这个等式两边的 $\boldsymbol{n}$ 是方向相反的.





>[!prp] 命题10（线性性）
>设 $\boldsymbol{f}$ 和 $\boldsymbol{g}$ 在定向的光滑曲面 $\Sigma$ 上的第二类曲面积分存在，则对任何常数 $\alpha, \beta$，$\alpha \boldsymbol{f}+ \beta \boldsymbol{g}$ 在 $\Sigma$ 上的第二类曲面积分也存在，且成立
>$$
>\iint_{\Sigma} (\alpha \boldsymbol{f} + \beta \boldsymbol{g}) \cdot \boldsymbol{n} \ \mathrm{d}S = \alpha \iint_{\Sigma} \boldsymbol{f} \cdot \boldsymbol{n} \ \mathrm{d}S + \beta \iint_{\Sigma} \boldsymbol{g} \cdot \boldsymbol{n} \ \mathrm{d}S.
>$$




>[!prp] 命题11（曲面可加性）
>设定向的光滑曲面 $\Sigma$ 分成了两片 $\Sigma_1$ 和 $\Sigma_2$，它们与 $\Sigma$ 的取向相同（这时记为 $\Sigma = \Sigma_1 + \Sigma_2$），如果向量值函数 $\boldsymbol{f}$ 在 $\Sigma$ 上的第二类曲面积分存在，则它在 $\Sigma_1$ 和 $\Sigma_2$ 上的第二类曲面积分也存在。反之，如果 $\boldsymbol{f}$ 在 $\Sigma_1$ 和 $\Sigma_2$ 上的第二类曲面积分存在，则它在 $\Sigma$ 上的第二类曲面积分也存在，且成立
>$$
>\iint_{\Sigma} \boldsymbol{f} \cdot \boldsymbol{n} \ \mathrm{d}S = \iint_{\Sigma_1} \boldsymbol{f} \cdot \boldsymbol{n} \ \mathrm{d}S + \iint_{\Sigma_2} \boldsymbol{f} \cdot \boldsymbol{n} \ \mathrm{d}S.
>$$
>利用性质 3 的思想就可以把第二类曲面积分的定义推广到分片光滑的曲面上去。

^d090ed




>[!prp] 例题12.1
>计算 $I = \displaystyle\iint_{\Sigma} (x+1)dydz + (y+1)dxdz + (z+1)dxdy$，其中 $\Sigma$ 为平面 $x+y+z=1$，$x=0$，$y=0$ 和 $z=0$ 所围立体的表面，方向取外侧.



>[!prp] 例题12.2
>计算 $\displaystyle\iint_{\Sigma} x^3 \ \mathrm{d}y\ \mathrm{d}z + y^3 \ \mathrm{d}z\ \mathrm{d}x + z^3 \ \mathrm{d}x\ \mathrm{d}y$，其中 $\Sigma$ 为上半椭球面 $\dfrac{x^2}{a^2} + \dfrac{y^2}{b^2} + \dfrac{z^2}{c^2} = 1$，$z \geq 0$（$a,b,c > 0$），方向取上侧.




>[!prp] 例题12.3
>计算 $\displaystyle\iint_{\Sigma} (z^2 + x) \ \mathrm{d}y\ \mathrm{d}z + \sqrt{z} \ \mathrm{d}x\ \mathrm{d}y$，其中 $\Sigma$ 为抛物面 $z = \dfrac{1}{2}(x^2 + y^2)$ 在平面 $z=0$ 与 $z=2$ 之间的部分，方向取下侧.









